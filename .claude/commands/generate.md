---
description: Generate one fresh exam-style MCQ on any topic or scenario. Retrieval-first — answer reveals nothing until you commit.
argument-hint: "<topic or scenario>  — e.g.:  'Customer Support Resolution Agent'  |  'tool selection'  |  D3"
---
Use the `question-generator` skill to generate one fresh exam-style question for: $ARGUMENTS

Present only the stem + four options (A–D). Reveal nothing — no answer, no hints — until the user commits to a letter. Then grade: state "Correct Answer: [Letter]", explain why the key applies the right principle, name each wrong option's failure mode, and cite an official doc.
